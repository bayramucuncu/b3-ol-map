import { WidgetHostDirective } from './widget-host.directive';

describe('WidgetHostDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetHostDirective();
    expect(directive).toBeTruthy();
  });
});
